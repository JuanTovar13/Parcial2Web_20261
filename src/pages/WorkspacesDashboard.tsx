import { List } from "../components/list"

export const WorkspaceDashboard =() => {

    return(
        <div>
            <h1>Workspace Dashboard</h1>
            <List/>
        </div>
    )
}